fastapi>=0.110.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.9
pillow>=10.0.0
pillow-heif>=0.16
numpy>=1.24.0
# Non-headless (not -headless) so it satisfies rapidocr-onnxruntime's opencv
# dependency with a SINGLE cv2 install — two opencv distros share the cv2/ dir
# and clobber each other non-deterministically. Railway needs libgl1 +
# libglib2.0-0 (railpack.json aptPackages) for non-headless cv2 to import.
# <5: scan pipeline is calibrated against 4.x (HoughLinesP output shape changed
# in 5.0; other 5.x behavior is unaudited).
opencv-python>=4.8.0,<5
pytesseract>=0.3.10
onnxruntime>=1.17
rapidocr-onnxruntime>=1.4  # PP-OCR number reading (far stronger than Tesseract)
httpx>=0.27.0
fastapi-users[sqlalchemy]>=15.0,<16
sqlalchemy[asyncio]>=2.0.30,<2.1
asyncpg>=0.29
alembic>=1.18,<2
rapidfuzz>=3.0
